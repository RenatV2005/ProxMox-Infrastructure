services:
  nginx-web-server:
    image: renat-nginx:v1
    ports:
     - "443:443"
    networks:
     - homelab-net
     - reverse-proxy-net
    restart: unless-stopped

networks:
  homelab-net:
  reverse-proxy-net:
   external: true
