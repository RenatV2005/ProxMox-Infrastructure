services:

  nginx-proxy:
    image: nginx
    ports:
     - 80:80
    volumes:
     - ./nginx-configs/true.conf:/etc/nginx/conf.d/default.conf

  nginx-app:
   image: nginx
   volumes: 
    - ./nginx-files:/usr/share/nginx/html
